# Deprecated

Use **[security-assessment-export.md](security-assessment-export.md)** for the current security assessment export format.

- **Endpoint:** `POST /api/v2/security-assessments/import`
- **Default schema:** `security` (set `AIRTASYSTEMS_EXPORT_SCHEMA=legacy` only for the old compliance import API)
